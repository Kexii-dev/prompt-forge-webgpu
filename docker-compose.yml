services:
  prompt-forge:
    build: .
    container_name: prompt-forge
    restart: unless-stopped
    ports:
      - "100.112.244.34:3010:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
